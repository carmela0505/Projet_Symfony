<form method="post" action="{{ path('app_marque_delete', {'idMarque': marque.idMarque}) }}" onsubmit="return confirm('{% trans %} Souhaitez-vous supprimer cette marque?{% endtrans %}'); ">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque.idMarque) }}">
     <button class="btn btn-danger">{% trans %}Supprimer{% endtrans %}</button>
</form>
