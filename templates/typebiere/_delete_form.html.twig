<form method="post" action="{{ path('app_typebiere_delete', {'idType': typebiere.idType}) }}" onsubmit="return confirm('{% trans %} Souhaitez-vous supprimer cette type de bière?{% endtrans %}'); ">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typebiere.idType) }}">
     <button class="btn btn-danger">{% trans %}Supprimer{% endtrans %}</button>
</form>
