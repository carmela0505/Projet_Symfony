{{ form_start(form) }}
<div class="form-group">

	{{ form_row(form.nomType, {'attr': {'class': 'form-control'},'label': 'Type de bière :' |trans}) }}
	<br>
</div><br>
<button class="btn btn-dark">{{ button_label|default('Enregistrer' |trans) }}</button>
{{ form_end(form) }}
